version: '3.8'

services:
  node1:
    build:
      context: ../node_app
      dockerfile: Dockerfile
    ports:
      - "18081:8080"
    environment:
      - NODE_NAME=node1
      - APP_VERSION=v1
    container_name: node1

  node2:
    build:
      context: ../node_app
      dockerfile: Dockerfile
    ports:
      - "18082:8080"
    environment:
      - NODE_NAME=node2
      - APP_VERSION=v1
    container_name: node2

  node3:
    build:
      context: ../node_app
      dockerfile: Dockerfile
    ports:
      - "18083:8080"
    environment:
      - NODE_NAME=node3
      - APP_VERSION=v1
    container_name: node3
